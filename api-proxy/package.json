{
  "name": "si-genie-proxy",
  "version": "10.6.0",
  "description": "SI Génie Consultant — Proxy Backend (SQLite + WebSocket + Stockage disque)",
  "type": "module",
  "main": "api-proxy.js",
  "scripts": {
    "start": "node scripts/ensure-sqlite.cjs && node api-proxy.js",
    "dev": "node --watch api-proxy.js",
    "setup": "npm install && node -e \"console.log('✅ Proxy prêt')\"",
    "pm2": "pm2 start api-proxy.js --name si-proxy --watch"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^12.10.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.0",
    "express-validator": "^7.2.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^8.0.4",
    "redis": "^4.6.7",
    "socket.io": "^4.7.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
